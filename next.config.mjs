/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "a.rina.place",
            "b.ppy.sh",
            "assets.ppy.sh"
        ],
    },
};

export default nextConfig;
